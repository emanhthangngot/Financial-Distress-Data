"""RAG helpers for src.llm.rag_pipeline (embedding backends, chunking, PGVector).

This package is where the RagIngestionService (src/llm/contracts.py) helpers
live; rag_pipeline.py re-exports the public surface it needs.
"""
