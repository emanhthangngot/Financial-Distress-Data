# Evidence placeholder — must NOT pass phase-08 audit
#
# Every metadata key from the evidence contract is present, but every value is
# blank. Presence of a key is not proof; the value must be non-empty.

- rubric_id: ML-evidence-empty-values
- execution_timestamp:
- source_sha:
- gitops_sha:
- versions:
- command:
- expected_result:
- actual_result:
- redaction_status:
