"""Phase 2 rubric contract and evidence tests.

These tests verify the rubric matrix, linter, and Phase 1 non-mutation
contract. They MUST FAIL before the linter / matrix source-of-truth
is implemented (SDD test-seed rule).
"""
