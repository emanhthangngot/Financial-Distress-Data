# CLAUDE.md
@AGENTS.md

## Claude Code

Skill catalog: use the installed `ak:*` skills (see runtime skill listing), not `.codex/skills/` — those are Codex-only and not loaded here.

Before acting, follow `AGENTS.md` → `Mandatory Skill Activation`: classify each request, read the selected skill's complete `SKILL.md`, announce it in the session progress update, and follow its workflow. For direct explanations or simple read-only answers, explicitly record when no skill is needed.
