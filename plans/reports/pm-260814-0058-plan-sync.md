# Plan sync — production hardening overlay

Plan: `plans/260813-1846-production-hardening-overlay/`
Branch: `feat/production-hardening-overlay`

## Reconciled state

The plan remains `in_progress`. The twelve phase files remain `in_progress` because
the live acceptance rows depend on external registry credentials, GKE quota, and
cluster operators that are not present in the connected cluster.

Eight locally verified acceptance rows are now checked:

| Phase | Checked | Remaining | Evidence |
|---|---:|---:|---|
| 1 | 3 | 16 | protected-path/artifact/matrix/LLM gates |
| 2 | 3 | 3 | GitOps validator, combined quality gate, LLM gate |
| 3 | 2 | 3 | catalog negative test, LLM gate |
| 4–12 | 0 | 61 | live quota, operators, runtime, soak and freeze pending |

The local implementation and verification slice is complete: full tests (311),
Ruff, Black, Compose config, Stage 1 gate, platform .ate, GitOps Helm/Terraform/
kubeconform validation, matrix strict, LLM executed audit, API container smoke,
and focused evidence tests all pass. The strict real-digest command is exposed
and fails closed on four unresolved API digests.

## Deliberately not marked complete

- ML executed audit: 57 rows remain `design_only`.
- GHCR publication/signing: current token lacks `write:packages`.
- Kyverno/ESO/Linkerd/KEDA/Argo Rollouts/Lakekeeper/Flink/MLflow live evidence:
  corresponding CRDs/workloads are absent from the connected cluster.
- Concurrent soak: one Ready node and `E2_CPUS=8` do not satisfy the planned
  24-vCPU capacity target.

No cluster mutation, quota/billing change, registry push, or synthetic evidence
was used to inflate the progress count.
